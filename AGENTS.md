# AGENTS.md — LMS 알리미 설치 안내 (AI 에이전트용)

사용자가 이 저장소 링크만 주면서 "설치해줘"라고 했다면, 이 문서 하나로 끝까지 진행할 수 있습니다.
사용자는 개발자가 아닐 가능성이 높습니다. 터미널 용어를 설명 없이 쓰지 마세요.

## 이 도구가 하는 일

한동대 LMS(Canvas)에 로그인해서 앞으로 며칠 안의 **과제·퀴즈·일정**과 **공지사항**을 가져와,
**새로 생긴 것만** 텔레그램으로 보냅니다. 사용자 컴퓨터에서만 돌아가고 서버는 없습니다.

## 0. 먼저 확인할 것

```bash
python3 --version    # 3.9 이상이어야 합니다
```

3.9 미만이거나 `python3`가 없으면, 설치를 진행하지 말고 사용자에게 Python 설치를 먼저 안내하세요.

## 1. 설치 + 실행 (이 한 줄이 전부)

```bash
git clone https://github.com/Dirini/lms-notifier.git
cd lms-notifier
python3 bootstrap.py
```

`bootstrap.py`가 가상환경 생성 → 패키지 설치 → 웹 서버 실행 → 브라우저 열기까지 합니다.
브라우저가 자동으로 안 열리면 `http://127.0.0.1:8912`를 열라고 안내하세요.

**이 명령은 서버가 떠 있는 동안 끝나지 않습니다(정상입니다).** 백그라운드로 돌리거나,
별도 터미널에서 실행하세요. 창을 닫으면 자동 확인도 같이 멈춥니다.

## 2. 절대 하지 말 것 (중요)

- **학번·히스넷 비밀번호·텔레그램 봇 토큰을 대신 입력하지 마세요. 채팅으로 묻지도 마세요.**
  이 값은 웹 화면에서 사용자 본인만 입력해야 합니다. 서버가 `127.0.0.1`에서만 열려서
  입력값이 그 컴퓨터 밖으로 안 나가는 구조인데, 에이전트가 대신 받으면 그 전제가 깨집니다.
- `.env`, `.local/`, `state.json`을 직접 만들거나 채우지 마세요. 사용자가 화면에서 연결하면
  서버가 알아서 만듭니다.
- 커밋·푸시할 일이 있다면 `.local/`, `.env`, `state.json`, `.venv/`가 안 섞였는지 확인하세요
  (`.gitignore`에 이미 있지만, 사용자 자격증명이 들어가는 파일이라 한 번 더 보세요).

## 3. 설치 후 사용자에게 안내할 순서

화면은 4단계 카드입니다. 앞 단계를 끝내야 다음이 열립니다.

1. **LMS 계정** — 학번과 히스넷 비밀번호를 넣고 "연결하기".
   그 자리에서 실제 로그인을 시도하므로, 성공하면 진짜 되는 겁니다.
   학교 SSO를 5단계 거쳐서 처음엔 몇 초~수십 초 걸릴 수 있습니다(멈춘 게 아닙니다).
2. **가져올 항목** (선택) — 기본은 전체입니다. "과목 불러오기"로 수강 과목을 불러와
   필요 없는 과목·유형을 끌 수 있습니다. 바꾸면 바로 저장됩니다.
3. **텔레그램 알림** — 봇이 없으면 텔레그램에서 `@BotFather` → `/newbot` 으로 만들고
   받은 토큰을 넣습니다. 그다음 "봇에게 메시지 보낸 뒤 chat_id 확인" 버튼을 누르면
   `chat_id`가 자동으로 채워집니다.
   봇 하나를 여러 명이 같이 써도 됩니다 — `chat_id`로 구분돼서 알림은 각자에게만 갑니다.
4. **지금 확인** — "지금 확인해서 보내기"로 즉시 테스트. 텔레그램에 오면 성공입니다.

## 4. 설정할 수 있는 것

- **자동 확인** — 끄기 / 반복 주기 / 정해진 시각. 이 프로그램이 실행 중일 때만 동작합니다.
- **알림 내용** — `간단히`(날짜+제목만, 제출한 과제는 제외) / `자세히`(분류·과목·마감·제출여부).

## 5. 자주 나오는 질문에 대한 정답

**Q. 컴퓨터를 꺼도 알림이 오나요?**
아니요. 이 프로그램이 실행 중일 때만 확인합니다. 꺼져 있던 동안의 예약 시각은 건너뜁니다
(다음 실행을 항상 현재 시각 기준으로 다시 잡습니다). 다시 켜면 화면 위에
"N번 확인하지 못했어요" 배너가 뜹니다.
**다만 글이 사라지진 않습니다** — 다음 확인 때 LMS를 다시 통째로 읽고 이미 보낸 것만
걸러내므로, 못 받은 글은 그때 함께 옵니다. 누락이 아니라 지연입니다.

**Q. 자동으로 시작되게 할 수 있나요?**
기본은 안 됩니다(자동 시작 설정 없음). 원하면 macOS는 launchd/cron, Windows는 작업 스케줄러에
`src/main.py`를 등록하면 서버 창 없이도 정해진 시각에 돌릴 수 있습니다. 컴퓨터 자체가 꺼져
있으면 그것도 안 돕니다.

**Q. AI 구독이 필요한가요?**
아니요. LLM을 전혀 호출하지 않습니다. 순수 Python 스크립트입니다.

**Q. 다른 대학교도 되나요?**
아니요. 지금은 **한동대 전용**입니다. 로그인이 한동대 SSO(`online.handong.edu`) 경로에
묶여 있습니다. 다른 학교는 로그인 방식이 달라 그대로는 동작하지 않습니다.

**Q. 윈도우에서 되나요?**
됩니다. `bootstrap.py`가 윈도우 가상환경 경로를 알아서 처리합니다.

**Q. 링크가 사람마다 다른가요?**
아니요. `http://127.0.0.1:8912`는 "내 컴퓨터"를 뜻하는 주소라 모두 같지만, 각자 자기
컴퓨터의 프로그램에 연결됩니다. 남에게 내 화면이 보이지 않습니다.

## 6. 안 될 때 확인 순서

| 증상 | 확인할 것 |
|---|---|
| 로그인 실패 | 히스넷 웹사이트에서 같은 학번/비번으로 직접 로그인되는지 |
| `n초째 확인 중` 에서 멈춤 | 학교 서버 응답 대기입니다. 15초 넘으면 어느 단계에서 멈췄는지 화면에 뜹니다 |
| 텔레그램이 안 옴 | 봇에게 먼저 아무 메시지나 보낸 적이 있는지 (없으면 `chat_id`를 못 잡습니다) |
| "새 소식 없음"만 뜸 | 정상입니다. 이미 보낸 항목은 다시 안 보냅니다 |
| 포트 충돌 | `8912`를 다른 프로그램이 쓰는 중입니다 |

터미널 로그에 각 로그인 단계가 몇 초 걸렸는지 남습니다. 문제 보고 시 그 로그와 화면 오류
문구를 함께 받으세요.

## 7. 구조 (수정이 필요할 때만)

```
bootstrap.py        설치 + 실행 진입점
src/server.py       로컬 웹서버(127.0.0.1:8912) + 자동 확인 스케줄러
src/lms_client.py   한동대 SSO 로그인 + Canvas API 조회
src/main.py         CLI 실행 경로 (--dry-run 지원) + 메시지 포맷
src/secrets_store.py  .local/secrets.json 읽기/쓰기
src/prefs.py        필터·주기·알림형식 설정
src/state.py        이미 보낸 항목 기록(중복 방지)
src/telegram.py     텔레그램 전송
web/                화면 (index.html · app.js · styles.css)
```

전송 없이 동작만 확인하려면:

```bash
.venv/bin/python3 src/main.py --dry-run --days 14
```
