# AGENTS.md — LMS 알리미 설치 안내 (AI 에이전트용)

이 저장소를 설치해달라는 요청을 받았다면 아래를 그대로 따르세요.

## 설치 + 실행 (한 번에)

```bash
python3 bootstrap.py
```

가상환경 생성, 패키지 설치, 웹 서버 실행까지 이 한 줄로 끝납니다. 실행되면
브라우저가 자동으로 열립니다 (`http://127.0.0.1:8912`). Python 3.9 이상이 필요합니다.

## 절대 하지 말아야 할 것

- **사용자의 학번, 히스넷 비밀번호, 텔레그램 봇 토큰을 절대 대신 입력하지 마세요.**
  채팅으로 이 값을 물어보지도 마세요. 이 정보는 웹 화면에서 사용자 본인이 직접
  입력해야만 합니다 — 이 저장소의 서버는 `127.0.0.1`에서만 열려서 입력한 값이 그
  컴퓨터 밖으로 전송되지 않는 구조인데, 에이전트가 대신 입력하면 그 전제가 깨집니다.
- `.env`, `.local/`, `state.json`을 직접 만들거나 값을 채우지 마세요 — 사용자가 웹
  화면에서 연결하면 서버가 알아서 만듭니다.
- 이 저장소에 커밋/푸시할 일이 있다면 `.local/`, `.env`, `state.json`, `.venv/`가
  포함되지 않았는지 확인하세요 (`.gitignore`에 이미 있음). 사용자 개인 자격증명이
  들어있는 파일입니다.

## 설치 후 사용자에게 안내할 것

1. 브라우저에 뜬 "LMS 계정" 카드에 본인 학번/히스넷 비밀번호를 입력해서 연결
2. 텔레그램 봇이 없으면 `@BotFather`로 새로 만들기 (README.md 2번 참고)
3. 텔레그램 연결까지 끝나면 "지금 확인해서 보내기"로 정상 동작 확인

자세한 사용법·문제 해결·자동 확인 주기 설정은 README.md를 참고하세요.
